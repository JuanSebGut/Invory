{
  "name": "api-gateway",
  "version": "1.0.0",
  "description": "API Gateway de INVORY â€” orquesta auth, users, categories, products, inventory y audit",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test --experimental-test-isolation=none \"tests/**/*.test.js\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "bcryptjs": "^3.0.3",
    "supertest": "^7.1.4"
  }
}
